# ajna-cloud SDK
# Installed from GitHub Release wheel (downloaded by CI — see SDK_VERSION in deploy workflow)
# Local dev: uv pip install -e ../ajna-cloud-sdk
# To upgrade: update SDK_VERSION in .github/workflows/deploy-backend.yml

# AWS
boto3>=1.28.0

# HTTP
requests>=2.28.0

# Auth (used by ajna-cloud SDK for Cognito JWT verification)
python-jose[cryptography]>=3.3.0

# AI
openai>=1.54.3
groq>=0.9.0

# Config & Utils
python-dotenv>=1.0.0
pytz>=2024.2
httpx>=0.27.2
PyJWT>=2.8.0
cryptography>=41.0.7
pydantic>=2.5.0

# Image processing (EXIF auto-rotation)
Pillow>=10.0.0
